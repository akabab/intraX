p {{welcome}}
p {{speach}}
ul
  li(ng-repeat="item in data")
    a(ng-href="#/category/{{item.name}}/") {{item.name}}
    ul
      li(ng-repeat="sub in item.sub")
        a(ng-href="#/category/{{item.name}}/{{sub.name}}") {{sub.name}}
p
